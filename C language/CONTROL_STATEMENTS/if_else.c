#include <stdio.h>
int main()
{
    int x;
    printf("enter the number:");
    scanf("%d", &x);

    if (x >= 10)
    {

        printf("number is greater than  10:");
    }

    else

        printf("number is less than 10:");
}